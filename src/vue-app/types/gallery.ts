/**
 * Type definitions for Gallery System (Vue App)
 * Re-exports from canonical type definitions
 */

// Re-export all gallery types from canonical location
export type {
  GalleryFilters,
  GalleryOption,
  ThemeScreenshot,
} from "../../types/gallery.js";
